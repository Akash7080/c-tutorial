#include<stdio.h>
void main()
{
    int x,m,k,l,p,z;
    m=sizeof(x);
    k=sizeof(float);
    l=sizeof(10);
    p=sizeof(2.5);
    z=sizeof('A');
    x=sizeof(char);
    printf("the value of m %d",m);
    printf("the value of n %d",m);
}